"""webplay — the launcher-based remote-play fork (browse + low-latency play).

Runs as its own app on a separate port/funnel from the MVP controller (backend/,
:8080), which it reuses for the gamepad via a control proxy.
"""
